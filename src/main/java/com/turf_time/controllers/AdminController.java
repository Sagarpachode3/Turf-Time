package com.turf_time.controllers;

public class AdminController {

	
	/*
	 * @GetMapping("/{managerId}/bookings") public ResponseEntity<List<BookingDto>>
	 * getBookingsByManagerId(@PathVariable Integer managerId) { List<BookingDto>
	 * bookings = managerService.getBookingsByManagerId(managerId); return new
	 * ResponseEntity<>(bookings, HttpStatus.OK); }
	 */
}
